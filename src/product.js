// The product name lives here and nowhere else in code. A rename is this
// line plus package.json (productName, appId), which electron-builder needs
// as static strings.
module.exports = {
  PRODUCT_NAME: 'Whisp',
};
